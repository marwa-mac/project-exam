module.exports = {
    content: [
      // Chemins vers tous vos fichiers HTML, JS, JSX, etc.
      "./src/**/*.{html,js,jsx,ts,tsx}", // Tous les fichiers dans src/
      "./public/**/*.html",              // Fichiers HTML dans public/
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }